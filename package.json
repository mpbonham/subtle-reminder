{
  "name": "subtle-reminder",
  "private": true,
  "scripts": {
    "i": "cd src && pip install -r requirements.txt && cd .. && yarn && cd client && yarn",
    "build": "cd client && yarn build",
    "start-client": "cd client && yarn start",
    "start-server": "cd src && python3 app.py"
  },
  "dependencies": {
    "axios": "^1.3.3",
    "dotenv": "^16.0.3"
  }
}
